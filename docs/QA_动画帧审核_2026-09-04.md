# QA 动画帧审核报告 2026-09-04（全量体检 · 只读）

> 工具：`tools/gen/audit_anim_pack.py --all --no-write` · 契约：`docs/动画_素材契约与审核_v1.md`
> 23 个在册帧包已打 `baseline_v1` 戳（引擎过渡），本次为质量基线快照，不改变任何戳。

## 结论

- 自动审核门禁已上线并验证：**引擎只播放 `REVIEW.json status=approved` 的帧包**（`UnitSprites.load_anim_frames` + 梦龙扇翅 Atlas 双门禁）；新包必须过审才能进游戏。
- baseline_v1 保护生效：会 FAIL 的历史包不被误覆写（实测 human/unit_6 保持 approved + baseline_v1）。
- 引擎改动经 S1 Autoplay 冒烟：SCRIPT ERROR 0，通过。
- 梦龙 14-17 因自定色板被误伤 → 已生成包级 `palette.json` 色板覆盖（从现有帧提取主色），复检转绿。

## 逐包结果

| 包 | 画布 | 结果 | 问题 |
| --- | --- | --- | --- |
| dragon/0,1,2 | 64×72/96×108 | ✅ | 1/2 有非透明角（警示）；0 fly 接缝 IoU 0.63 |
| dragon/3 | 96×108 | ⚠️ | **色板离群 25%**；fly 接缝 IoU 0.65 |
| dragon/14-17 梦龙 | 96×108 | ✅ | palette.json 覆盖后通过；扇翅表完整 |
| fungus/0-3 | 64×72 | ✅ | 2 号 death 底漂 3.1px（警示） |
| human/0,2,3,4,8 | 96×108 | ✅ | death 底漂 2.5~5.4px；0/1 fly 接缝 0.61/0.54 |
| human/1 | 96×108 | ✅ | fly 接缝 IoU 0.54（最接近阈值） |
| human/5 | 96×108 | ⚠️ | **色板离群 45%**（最重）；death 底漂 3.1px |
| human/6 | 96×108 | ⚠️ | **death 底漂 8.2px**；色板离群 28% |
| silicon/0-2 | 64×72 | ✅ | 2 号 death 底漂 3.1px（警示） |

## 已知缺口 / 行动清单（按优先级）

1. **敌族（enemies）无任何动画帧包** — 当前敌兵只能 base 静帧。→ 下一个生成任务：敌族每兵种 ≥ idle/walk/attack 包。
2. **human/5、human/6、dragon/3 色板离群** — 需重新过 pixelize 量化或确认自定色板后补 `palette.json`。
3. **多个包 death 底漂 2.5~8.2px** — 死亡动画脚底滑动/跳动；影响小但追求丝滑应重出。
4. **fly 接缝 IoU 0.54~0.65（阈值 0.65）** — 4 帧循环落点可见；升级到 6-8 帧或补对称帧。
5. **fungus/silicon/dragon-0/2 仍为旧 64×72 画布** — 可运行，非新包标准；视觉升级时统一 96×108。

## 验收路径（新包标准）

生成（Cursor/ComfyUI）→ `audit_anim_pack.py` PASS → REVIEW.json=approved → Godot F5 实际播放无跳变 → 入库。
