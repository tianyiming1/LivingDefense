# 变更单 CO-019：随时随机事件总线（可扩展）

- **状态**：已实施（首包）
- **日期**：2026-09-03
- **派工**：WP-EVT1 / 派工_WP_可玩性丰富度.md
- **原则**：事件是好玩支柱之一，不是整局唯一内容；池可扩展

## 变更

1. `event_bus.gd`：数据表 `POOL` 驱动；冷却；同屏 1 个进行中；同名局内上限。
2. 首包 **8** 条样例（≥6 类）：
   - 天气：雷暴劈敌/误伤
   - 机缘：单位顿悟攻速
   - 挖宝：可点击宝点（金/补给/空/下波伏兵）
   - 危机：跑者潮、补给被盗（人族）
   - 战场：大风敌加速
   - 抉择：血金（Y/N）、游商进化折扣（Y/N）
3. 接入 `weather_controller` 视觉；状态句 + 音效。
4. 波间挖宝惊怪 → `pending_spawns` 注入下一波。

## 操作

- 抉择事件：键盘 **Y** 接受 / **N** 拒绝  
- 挖宝：左键点金色「?」点

## 影响文件

`event_bus.gd`、`event_treasure.gd`、`main.gd`、`translations/strings.csv`、本变更单

## 后续

策划 `事件池_v1.md` ≥12 条 → 按行加入 `POOL` 即可扩展，勿改总线。
