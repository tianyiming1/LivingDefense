# 变更单 CO-023：事件二包 + 近终点预警 + 遗物替换

- **状态**：已实施
- **日期**：2026-09-03

## 变更

1. **终点震颤** `end_rumble`：波中且有敌进度≥0.72 才可抽中；近终点敌加速；每波最多 1 次。
2. **进化潮汐** `evolve_tide`：一段时间进化折扣（免费机缘，非游商付费）。
3. **近终点预警**（常驻轻提示）：进度≥0.85 时状态句提醒集火。
4. **遗物满仓替换**：Y 弃最早一件换新 / N 拒收得补偿金（替代纯拒收）。

## 影响文件

`event_bus.gd`、`run_items.gd`、`main.gd`、`translations/strings.csv`
