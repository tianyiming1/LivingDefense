# AUDIT — frost candidates (continuous pass 2026-09-03B)

**Verdict: FAIL — block all promote to `approved/` / `picks/`**

## Scope
`longren/frost/candidates/` `001`–`010` (`*_game.png` + `*_raw.png` where present)

## Measured color (game sprites)
| id | warm | cool | note |
|----|------|------|------|
| 001–010 | 50–64% | **0%** | orange/purple block prototype |

Frost SPECIES requires **blue-white base + navy membranes**. Cool pixel ratio must be dominant; **0% cool = hard fail**.

## Hard fails (every id)
1. **Species color** — magma orange body, not ice blue-white
2. **Style lock** — flat geometric blocks, not pick_004 molten crystalline / jagged voxel scales
3. **Gender/design** — not female frost; no pretty face / bust read
4. **Silhouette** — stick limbs / block torso; not subspecies shape differentiation
5. **Process** — looks like `draw_longren_elemental` placeholder dumped into frost folder without STYLE_GATE / create-game-assets gate

## Wings
1 pair back wings present (anatomy wing rule OK) — **insufficient alone**.

## Required action
- Do **not** copy to `approved/` or `picks/`
- Quarantine or delete FAIL batch; regenerate under `SPECIES.md` + `STYLE_GATE_pick004.md` + `ANATOMY.md` + `create-game-assets` / `asset-architecture`
- Catalog must not claim deleted while files remain

## Skills expected (not evidenced for this batch)
`router` → `create-game-assets` / `asset-architecture` / `directing-game-visuals` / `tower-defense` before write
