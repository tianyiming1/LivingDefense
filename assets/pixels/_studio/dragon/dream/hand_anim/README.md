# 梦龙手绘分帧工作区

未过审帧**禁止**进 `assets/pixels/dragon/unit_*_anim/`。

```bash
python tools/gen/gen_dream_hand_templates.py --units 14
# 画完后：
python tools/gen/promote_dream_hand_anim.py --unit 14
```

见 `../KEYFRAMES.md`。FAIL 自生成 / WorkBuddy 已删除，勿复活。
