# Audio assets

W1（CO-ART-PROD-001）默认 **全部程序合成**，由 `scripts/audio_controller.gd` + `audio_synth.gd` 运行时生成。

| 来源 | 许可 | 说明 |
|------|------|------|
| 本仓库程序合成 | 自有 | 可进正式包 / Steam；无第三方采样 |

若日后引入外部 BGM/SFX：仅 CC0 或已购商用授权；许可证副本与 `PROVENANCE` 登记后方可进 `assets/audio/` ship 路径。见 `docs/ASSET_POLICY.md`。
